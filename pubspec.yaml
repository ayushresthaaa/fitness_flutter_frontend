name: fitness_app
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.0
  http: ^1.6.0
  provider: ^6.0.5
  cupertino_icons: ^1.0.8
  url_launcher: ^6.1.10
  google_sign_in: ^6.1.4
  flutter_secure_storage: ^9.2.2 # Update from ^8.0.0
  formz: ^0.8.0
  introduction_screen: ^4.0.0
  shared_preferences: ^2.5.4
  dio: ^5.7.0
  fl_chart: ^0.69.0
  image_picker: ^1.2.1
  socket_io_client: ^2.0.3
  khalti_checkout_flutter: ^1.0.0-dev.9
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
